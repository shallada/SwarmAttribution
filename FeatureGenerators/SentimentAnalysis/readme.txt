How to run opinionfinder
------------------------

The RunOpinionFinder.sh script (which requires the folder name of the dataset) takes care of all of it.

All of it means the script creates the doc list, then processes the docs, then moves the results to the results area, and finally processes the results to create feature vectors.
